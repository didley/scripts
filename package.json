{
  "name": "scripts",
  "license": "UNLICENSED",
  "devDependencies": {
    "@types/node": "^20.6.0",
    "prettier": "^3.0.3",
    "ts-node": "^10.9.1",
    "typescript": "*"
  },
  "dependencies": {
    "eslint": "^8.0.1"
  },
  "scripts": {
    "typeCheck": "$npm_execpath tsc",
    "format": "$npm_execpath prettier . --write",
    "helloWorld": "$npm_execpath run ts-node ./src/helloWorld.ts"
  }
}